BOTS = {

    startxp = {

		fire_only_once = yes

		icon = oppression
		available = {
            if = {date > 1936.1.7}
            is_ai = yes
            has_war = no
            has_civil_war = no
		}
		visible = {
            is_ai = yes
		}
        ai_will_do = {base = 1000}
        cost = 0
        complete_effect = {
            army_experience = 10
        }
    }

    set_grand_doctrine_36= {

    fire_only_once = yes

     ai_will_do = {base = 1000}

            icon = oppression

            available = {
                is_ai = yes
                 has_war = no
                has_civil_war = no
            }
            visible = {
                is_ai = yes
            }
                complete_effect = {
                    set_grand_doctrine = new_battlefield_support
                    set_grand_doctrine = superior_firepower
                    set_grand_doctrine = new_convoy_raiding
                    set_sub_doctrine = capital_hunters
                    set_sub_doctrine = torpedo_primacy
                    set_sub_doctrine = light_task_forces
                    set_sub_doctrine = line_of_battle
                    set_sub_doctrine = air_subdoctrine_escort_fighter
                    set_sub_doctrine = air_subdoctrine_flying_artillery
                    set_sub_doctrine = air_subdoctrine_long_range_escort
                    set_sub_doctrine = air_subdoctrine_deep_air_raids
                            add_mastery = {
                                amount = 200
                                folder = air
                            }
                                add_mastery = {
                                amount = 150
                                folder = naval
                            }

                        }
    }
       set_sub_doctrine_37 = {

    fire_only_once = yes

     ai_will_do = {base = 1000}

            icon = oppression

            available = {
                if = {date > 1937.1.1}
                is_ai = yes
                has_civil_war = no
            }
            visible = {
                is_ai = yes
            }
                complete_effect = {
                    set_sub_doctrine = large_unit_tactics

                                add_mastery = {
                                amount = 150
                                track = infantry
                            }
                        }
    }
           set_sub_doctrine_38 = {

    fire_only_once = yes

     ai_will_do = {base = 1000}

            icon = oppression

            available = {
                if = {date > 1938.1.1}
                is_ai = yes
                has_civil_war = no
            }
            visible = {
                is_ai = yes
            }
                complete_effect = {
                     set_sub_doctrine = rapid_domination
                                add_mastery = {
                                amount = 150
                                track = operations
                            }
                        }
    }
          set_sub_doctrine_39 = {

    fire_only_once = yes

     ai_will_do = {base = 1000}

            icon = oppression

            available = {
                if = {date > 1939.1.1}
                is_ai = yes
                has_civil_war = no
            }
            visible = {
                is_ai = yes
            }
                complete_effect = {
                  if = { limit = {is_major = yes}
                    complete_special_project = {
						project = sp:sp_land_super_heavy_howitzer
					}
                }
                     set_sub_doctrine = field_engineering
                                add_mastery = {
                                amount = 150
                                track = combat_support
                            }
                        }
    }
              set_spec_doctrine_40 = {

    fire_only_once = yes

     ai_will_do = {base = 1000}

            icon = oppression

            available = {
               if = { date > 1940.1.1}
                is_ai = yes
                has_civil_war = no
            }
            visible = {
                is_ai = yes
            }
                complete_effect = {
                     set_sub_doctrine = field_engineering
                    set_grand_doctrine = special_forces_quality
                        }
    }
        set_spec_doctrine_41 = {

    fire_only_once = yes

     ai_will_do = {base = 1000}

            icon = oppression

            available = {
               if = { date > 1941.1.1}
                is_ai = yes
                has_civil_war = no
            }
            visible = {
                is_ai = yes
            }
                complete_effect = {
                    set_sub_doctrine = mountaineers_1 
                              add_mastery = {
                                amount = 90
                                folder = special_forces
                            }
                        }
    }
      set_grand_doctrine_test = {

    fire_only_once = yes

     ai_will_do = {base = 1000}

            icon = oppression

            available = {
                always = no
            }
            visible = {
               always = no
            }
                complete_effect = {
                    set_grand_doctrine = new_battlefield_support
                    set_grand_doctrine = superior_firepower
                    set_grand_doctrine = new_convoy_raiding
                    set_grand_doctrine = special_forces_quality
                    set_sub_doctrine = large_unit_tactics
                    set_sub_doctrine = rapid_domination
                    set_sub_doctrine = siege_artillery
                    set_sub_doctrine = armored_spearhead
                    set_sub_doctrine = capital_hunters
                    set_sub_doctrine = torpedo_primacy
                    set_sub_doctrine = light_task_forces
                    set_sub_doctrine = line_of_battle
                    set_sub_doctrine = mountaineers_1 
                    #set_sub_doctrine = rangers_2
                    set_sub_doctrine = air_subdoctrine_dogfighting_mastery
                    set_sub_doctrine = air_subdoctrine_dive_bombers
                    set_sub_doctrine = air_subdoctrine_operational_air_support
                    set_sub_doctrine = air_subdoctrine_heavy_aircraft_focus
                                add_mastery = {
                                amount = 150
                                folder = land
                            }
                            add_mastery = {
                                amount = 200
                                folder = air
                            }
                                add_mastery = {
                                amount = 150
                                folder = naval
                            }
                                add_mastery = {
                                amount = 90
                                folder = special_forces
                            }
                      if = { limit = {is_major = yes}
                    complete_special_project = {
						project = sp:sp_land_super_heavy_howitzer
					}
                }
                        }
    }
    bots_spr_attache = {

		fire_only_once = yes

		icon = oppression
		available = {
            OR = {
                original_tag = GER
            NOT = { 
                OR = {
                    has_idea = partial_economic_mobilisation
                    original_tag = USA
                    has_idea = war_economy
                    has_idea = tot_economic_mobilisation
                    has_idea = national_defense_state
                    has_idea = JAP_war_communism_idea
                    has_idea = totaler_krieg_economy
                    }
                }
            }
                AND = {
                    is_ai = yes
                    SPR = { has_civil_war = yes }
                }
            }
		visible = {
            is_ai = yes
		}
        ai_will_do = {base = 1000}
        cost = 0
        complete_effect = {
            add_war_support = 0.1
            add_command_power = -50
            if = {limit = {has_government = democratic} add_ideas = partial_economic_mobilisation}
            if = {limit = {has_government = fascism} if = {limit = { has_war_support < 0.4} add_ideas = partial_economic_mobilisation }}
            else_if = { limit = { has_war_support > 0.4} add_ideas = war_economy }
            if = {limit = {has_government = communism} if = {limit = { has_war_support < 0.4} add_ideas = partial_economic_mobilisation}}
            else_if = {limit = { has_war_support > 0.4}  add_ideas = war_economy }
            if = {limit = {has_government = neutrality} add_ideas = partial_economic_mobilisation }
        }
    }
}

                